package digital.playbeat.adminapp;

/** Build identity (no Gradle build — values kept in sync with AndroidManifest.xml). */
public final class BuildConfig {
    public static final String APP_VERSION = "3.1.0";
    public static final int VERSION_CODE = 4;
    public static final String APPLICATION_ID = "digital.playbeat.adminapp";
    public static final boolean DEBUG = false;

    private BuildConfig() {}
}
